name: codecov
on:
  push:
    branches: [master]
  pull_request: 
    branches: [master]
jobs:
  build:
    name: ubuntu-x86-cpu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: maboesanman/rust-codecov@v0
  
